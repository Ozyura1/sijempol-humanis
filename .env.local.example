# Environment variables for local development
NEXT_PUBLIC_API_URL=http://localhost:8000/api

# Server-side proxy target for Next.js API routes.
# On the VPS this can stay internal, so browsers do not need direct access to port 8000.
BACKEND_INTERNAL_API_URL=http://127.0.0.1:8000/api
